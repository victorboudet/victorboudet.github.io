onkeydown = function(event) {
    let = keyPr = event.keyCode;
    if (keyPr === 49)
        window.location.href = "../bounce/bounce.html";
    if (keyPr === 50)
        window.location.href = "../moove/moove.html";
}
